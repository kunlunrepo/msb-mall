# 端口
server:
  port: 8030

spring:
  # 项目名称
  application:
    name: mall-order
  # 数据源配置
  datasource:
    url: jdbc:mysql://192.168.10.50:3306/mall_oms?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  # 注册中心
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 192.168.10.50:8848


# nacos配置 可以先配置也可以不配置
#user:
#  userName: zhangsan
#  age: 18